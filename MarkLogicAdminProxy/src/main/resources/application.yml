server:
  port: 8080

marklogic:
  host: localhost
  username: admin
  password: admin
  schema: http

spring:
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
